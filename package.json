{
  "name": "wedding-pic-upload",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "gen:imgs": "node scripts/gen-imgs-manifest.js",
    "dev": "npm run gen:imgs && vite",
    "build": "npm run gen:imgs && vite build",
    "build:cf": "npm run gen:imgs && vite build --mode cloudflare",
    "preview": "vite preview"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "vue": "3.2.47",
    "vue-router": "^4.1.6"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.4",
    "jimp": "^0.16.13",
    "vite": "^2.9.18"
  }
}
